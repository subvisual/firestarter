language: ruby
rvm:
  - 1.9.3
  - 2.1.5
matrix:
  allow_failures:
    - rvm: 1.9.3
before_install:
  - "echo '--colour' > ~/.rspec"
  - git config --global user.name 'Travis CI'
  - git config --global user.email 'travis-ci@example.com'
install:
  - bundle install
notifications:
  email: false
